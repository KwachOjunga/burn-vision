#![allow(unused)]

use pyo3::prelude::*;
use burn_vision::burn::{
    optim::*,
    nn
};

pub mod layers {
    /// core layer types
    
    
    pub mod activations {}
    pub mod optimizers {}
    /// traits to identify layers
}
